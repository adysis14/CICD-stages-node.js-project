on: push
jobs:
  build-node:
    runs-on: ubuntu-latest
    container: node:16
    steps:
      -run: node --version
      -run: npm --version
      -uese: actions/checkout@V3
      -run: npm install
      -run: node app.js